//! Project test runner detection and execution.
//!
//! Filled in in phase 10. Despite the module name, this is *not* the crate's
//! integration test directory (that lives at `<crate-root>/tests/`).
